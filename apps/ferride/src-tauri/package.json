{
  "name": "@ferride/ferride",
  "private": true,
  "scripts": {
    "build:linux": "NO_STRIP=true tauri build",
    "build:win64": "tauri build --runner cargo-xwin --target x86_64-pc-windows-msvc",
    "dev:linux": "tauri dev",
    "dev:win64": "tauri dev --runner cargo-xwin --target x86_64-pc-windows-msvc"
  },
  "devDependencies": {
    "@tauri-apps/cli": "catalog:tauri"
  }
}
